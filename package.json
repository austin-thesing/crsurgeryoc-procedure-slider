{
  "name": "procedure-slider",
  "version": "1.0.0",
  "description": "Procedure slider with Swiper.js for Webflow",
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun run build.js --watch",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "swiper": "^11.1.14"
  },
  "devDependencies": {
    "bun-types": "latest",
    "csso-cli": "^4.0.2"
  }
}
